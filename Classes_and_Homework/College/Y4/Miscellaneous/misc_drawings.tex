\documentclass[10pt]{mypackage}

% sans serif font:
%\usepackage{cmbright}
%\usepackage{sfmath}
%\usepackage{bbold} %better blackboard bold

%serif font + different blackboard bold for serif font
\usepackage{newpxtext,eulerpx}
\renewcommand*{\mathbb}[1]{\varmathbb{#1}}
\renewcommand*{\hbar}{\hslash}

\pagestyle{fancy} %better headers
\fancyhf{}
\rhead{Avinash Iyer}
\lhead{}

\setcounter{secnumdepth}{0}

\begin{document}
\RaggedRight
\begin{tikzpicture}
    % Draw the real axis
    \draw[thick,->] (-4,0) -- (4,0);

    % Draw the imaginary axis
    \draw[thick,->] (0,-0.5) -- (0,3.5);

    % Draw the semicircular contour C
    \draw[thick,->] (4,0) arc[start angle=0, end angle=180, radius=4];
    \node at (2,2.5) {$C$};

    % Draw the small semicircular contour C_-
    \draw[thick,->] (0.5,0) arc[start angle=0, end angle=180, radius=0.5];
    \node at (0.5,0.5) {$C_-$};

    % Mark the pole inside the small semicircle
    \fill (0.5,0) circle[radius=1.5pt];
    \node[below right] at (0.5,-0.1) {\small $\bullet$};

    % Mark the other poles with "x"
    \node at (-2,1.5) {\Large $\times$};
    \node at (2,1.5) {\Large $\times$};
    \node at (-1,2.5) {\Large $\times$};
    \node at (1,2.5) {\Large $\times$};

\end{tikzpicture}
\end{document}
